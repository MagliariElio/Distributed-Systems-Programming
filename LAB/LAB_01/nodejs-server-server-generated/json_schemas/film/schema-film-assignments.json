{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "#/schema-film-base",
  "title": "Film Schema",
  "type": "array",
  "description": "Base schema for film data, containing essential film details.",
  "additionalProperties": false,
  "items": {
    "type": "object",
    "properties": {
      "filmId": {
        "type": "integer",
        "description": "Unique identifier of the film to be reviewed."
      },
      "reviewerId": {
        "type": "integer",
        "description": "Unique identifier of the user who has received the review invitation."
      }
    },
    "required": ["filmId", "reviewerId"]
  }
}
