{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "#/schema-user",
  "title": "User Account Schema",
  "description": "Schema representing a user account in the Film Manager service, including necessary authentication details and optional user information.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the user within the Film Manager service.",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "description": "Username chosen by the user."
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "User's email address, required for authentication."
    },
    "password": {
      "type": "string",
      "minLength": 6,
      "maxLength": 20,
      "description": "User's password, used for authentication (must be between 6 and 20 characters long)."
    }
  },
  "required": ["email"]
}
