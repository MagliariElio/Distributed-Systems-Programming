{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "#/schema-edit-review-request-create",
  "title": "Edit Review Request Schema Create",
  "type": "object",
  "description": "Schema used to create a new edit review request resource.",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path to the JSON Schema to use for the validation of the JSON data structure describing the edit review request."
    },
    "deadline": {
      "type": "string",
      "format": "date-time",
      "description": "Deadline for the edit review request (date, time, and timezone included)."
    }
  },
  "required": ["deadline"]
}
